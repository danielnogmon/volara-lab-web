Volara Lab — legal pages hub, served at volara.dev. One subfolder per app
(volara-log/, booknest/); root-level files are either the cross-app hub
(index.html) or legacy redirect stubs to volara-log/'s pages.

